{
  "version": 2,
  "builds": [
    {
      "src": "server/index.ts",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/index.ts"
    },
    {
      "src": "(/.*)",
      "dest": "/client/"
    }
  ]
}
